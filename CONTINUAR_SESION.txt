================================================================================
CONTINUAR SESIÓN — Tienda Virtual (compra de ropa)
Fecha de corte: 2026-07-24 (actualizado tras implementar la sincronización)
================================================================================

PROYECTO
--------
Ruta: D:/software/MisProyectos/tienda virtual - compra de ropa
Tienda virtual de ropa: catálogo público + carrito + pago por QR con comprobante
y verificación manual del dueño. Sin cuentas de cliente (nombre + WhatsApp).
Stack: React 19 + Vite + Tailwind 4 · Cloudflare Pages · Pages Functions (API)
· D1 (SQLite) · R2 (fotos y comprobantes) · SheetJS (xlsx) solo en el admin.
Producción: https://tienda-virtual-26n.pages.dev (deploy automático al hacer push
a main en GitHub ianvs007/tienda-virtual).
Docs del repo: ARQUITECTURA.md, BITACORA.md, README.md,
PROPUESTA_SINCRONIZACION.md (diseño de la sync de stock).

TRABAJO YA HECHO (2026-07-24) — NO REPETIR
------------------------------------------
1) Ocho mejoras del procedimiento de pago (commit 238d731, migración 002 aplicada
   local y remota): expiración de pedidos a 24 h con reposición de stock,
   referencia de pago para la glosa, badge "por verificar", aclaración de envío
   a coordinar, WhatsApp prefijado, idempotencia en checkout, rate limiting,
   y detalles menores (borrado de comprobante anterior, polling de estado,
   aviso de cancelado). Verificado y en producción.

2) Sincronización de stock con el sistema local offline (opción A, Excel,
   cierre de caja diario) — IMPLEMENTADA y probada localmente:
   - Migración 003_sincronizacion.sql: products.codigo (único, opcional) +
     settings.ultima_sincronizacion. Aplicada LOCAL; pendiente --remote.
   - functions/lib/sincronizar.js: stock nuevo = stock Excel − ventas en línea
     desde última sync (pedidos no cancelados; pendiente_pago cuenta).
   - Endpoints admin: POST /api/admin/sincronizar (aplicar, recalcula fresco),
     POST /api/admin/sincronizar/previsualizar, GET /api/admin/sincronizar/ventas.
   - Campo "Código del sistema local" en formulario de prendas y en las APIs
     de productos (alta pública con auth, alta/edición admin; 409 si duplicado).
   - Página /admin/sincronizar (src/pages/admin/Sincronizar.jsx): lee Excel con
     SheetJS (import dinámico), mapeo flexible de encabezados (acentos/alias),
     vista previa con advertencias, confirmar, descargar Excel de ventas.
   - Pestaña "🔄 Sincronizar" en el menú del admin.
   Pruebas de humo OK: cruce por código+talla+color, código no encontrado,
   sobreventa (clamp a 0), variante sin coincidencia, aplicar actualiza stock
   y ultima_sincronizacion, ventas queda vacío tras sync, 401 sin sesión.

PENDIENTE INMEDIATO AL RETOMAR
------------------------------
Hay cambios SIN COMMIT listos para publicar (verificados con npm run build OK):
- Layout.jsx lee el nombre de la tienda de /api/ajustes (nombre_tienda) y lo
  muestra en la cabecera y en el título de la pestaña; fallback "Tienda Virtual".
- src/pages/NoEncontrado.jsx (nuevo) + ruta catch-all path="*" en App.jsx
  (dentro del Layout) → página 404 pública.
Publicar: git add/commit/push a main (deploy automático). No hay migraciones
nuevas. Tras el push, marcar como cerrados esos dos ítems en BITACORA.md.

Resto de pendientes:
- Cuando el cliente tenga un Excel REAL de su sistema local: ajustar el mapeo
  de columnas en src/pages/admin/Sincronizar.jsx si hace falta.
- Pregunta abierta menor: ¿el sistema local puede IMPORTAR el Excel de ventas
  en línea, o es solo para registro manual?
- Operación pendiente del dueño: poner el código del sistema local a cada
  prenda en /admin → Prendas (necesario para que la sincronización cruce).

PROCEDIMIENTO DE TRABAJO (según BITACORA.md)
--------------------------------------------
- Cambios en la BD: migración numerada en migrations/ + apply --local (pruebas)
  y --remote (producción).
- Verificación local: npm run build + npx wrangler pages dev dist.
- Publicar: git add/commit/push a main → deploy automático en Cloudflare.

OTROS PENDIENTES MENORES (Fase 5, sin aprobar aún)
--------------------------------------------------
- Estreno real: crear admin, subir QR, cargar prendas, compra de prueba.
- SEO/Open Graph, PWA, dominio propio (opcional).
- Nombre de tienda en la cabecera leyéndolo de ajustes (hecho el 2026-07-24:
  Layout.jsx consulta /api/ajustes → nombre_tienda, con fallback y document.title).
- Página 404 pública (hecho el 2026-07-24: NoEncontrado.jsx + ruta "*" en App.jsx).
================================================================================
