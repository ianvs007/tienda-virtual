================================================================================
CONTINUAR SESIÓN — Tienda Virtual (compra de ropa)
Fecha de corte: 2026-09-04 (auditoría integral; main = origin/main = producción)
================================================================================

PROYECTO
--------
Ruta: D:/software/MisProyectos/tienda virtual - compra de ropa
Tienda virtual de ropa: catálogo público + carrito + pago por QR con comprobante
y verificación manual del dueño. Sin cuentas de cliente (nombre + WhatsApp).
Stack: React 19 + Vite + Tailwind 4 · Cloudflare Pages · Pages Functions (API)
· D1 (SQLite) · R2 (fotos y comprobantes) · SheetJS (xlsx) solo en el admin.
Producción: https://tienda-virtual-26n.pages.dev (deploy automático al hacer push
a main en GitHub ianvs007/tienda-virtual).
Docs del repo: ARQUITECTURA.md, BITACORA.md, README.md,
PROPUESTA_SINCRONIZACION.md.

ESTADO AL CORTAR
----------------
- main sincronizada con origin/main; último commit f049d84 DESPLEGADO en
  producción (verificado con `wrangler pages deployment list`).
- Árbol casi limpio: solo antes_sync.json sin versionar (scratch del comparador
  POS↔nube del 22/08).
- 8/8 tests (`node --test "functions/**/*.test.js"`) + npm run build OK.
- BD viva: products.global_id con backfill completo (2410/2410) e índice único
  idx_products_global_id presente.
- ⚠️ DRIFT: el ledger d1_migrations registra solo 001–004; la migración 005 se
  aplicó a mano sin registrar (ver PENDIENTE A).

TRABAJO RECIENTE (19–22/08/2026, vía coder.qwen.ai, ya en main y producción)
------------------------------------------------------------------------------
1) PR #2 y #3 (4da218c, 92de418): sync autoritativa del POS — reactivación de
   prendas tocadas por la sync + protocolo robusto start/commit.
2) Rediseño por identidad estable:
   - 65e4cee: migración 005_global_id.sql — el cruce pasa de codigo (mutable)
     a global_id (UUID estable); adopción del globalId del POS + backfill
     aleatorio para el legado.
   - d5f335f: escrituras del upsert batcheadas (CPU del Worker).
   - 1f058ff: los conflictos se resuelven reemplazando el registro de la nube
     por el canónico del POS (antes solo se salteaban).
   - f049d84: vaciado total del catálogo cloud desde el admin
     (POST /api/admin/productos/eliminar-todas).

PENDIENTE / PRÓXIMOS PASOS
--------------------------
A. Registrar la migración 005 en el ledger (con aprobación de Alain; solo
   inserta el registro, no toca datos):
   npx wrangler d1 execute tienda-virtual-db --remote --command "INSERT INTO d1_migrations (name) VALUES ('005_global_id.sql');"
   HASTA ENTONCES: no ejecutar `wrangler d1 migrations apply --remote`.

B. La sync por global_id requiere el POS actualizado y aún no hay zip de
   despliegue con globalId (proyecto tienda de ropas; CLAUDE.md item 17).

C. Fase 5 (sin aprobar): estreno real completo (admin, QR, compra de prueba),
   PWA, dominio propio.

PROCEDIMIENTO DE TRABAJO (según BITACORA.md)
--------------------------------------------
- Cambios en la BD: migración numerada en migrations/ + apply --local y --remote
  (OJO: aplicar el fix A antes de volver a usar migrations apply).
- Verificación local: npm run build + npx wrangler pages dev dist.
- Publicar: git add/commit/push a main → deploy automático en Cloudflare;
  verificar con `npx wrangler pages deployment list --project-name=tienda-virtual`.
================================================================================
